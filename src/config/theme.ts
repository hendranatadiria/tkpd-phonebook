import { Roboto } from "next/font/google";

export const font = Roboto({
  subsets: ["latin"],
  weight: ['400','500', '700', '900'],
});

export const colors = {
  primary: "#41b549",
  textPrimary: "#3d3d3f"
};